machine:
  python:
    version: 2.7.11
  environment:
    # Used for testing
    EXPECTED_PYTHON_VERSION: 2,7,11
    EXPECTED_PYTHON_ARCH: 64

dependencies:
  override:
    - pip install -U scikit-ci==0.5.0
    - ci before_install
    - ci install

test:
  override:
    - ci before_build
    - ci build
    - ci test

deployment:
  master:
    branch: master
    commands:
      - ci after_test